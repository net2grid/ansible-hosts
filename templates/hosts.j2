# Generated by net2grid.hosts

# Localhost
127.0.0.1 localhost {{ ansible_hostname }}

# Ansible config
{{ ansible_default_ipv4.address }} {{ ansible_hostname }}.{{ devbox.domain }} {{ ansible_hostname }}

# Playbook
{% if hosts is defined %}
{{ ansible_default_ipv4.address }} {% for host in hosts %}{{ host }} {% endfor %}
{% endif %}
